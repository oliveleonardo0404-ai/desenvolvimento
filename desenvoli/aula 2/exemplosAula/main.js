const valores = require('./calculo') // "./" serve para indicar o diretório atual ao importar arquivos, componentes ou módulos locais(traduzindo serve para indicar qual arquivo buscar no local)

valores.nomeIdade() // o "valores>>.<<nome serve para preencher a variavel valores com a variavel nome de calculo js"

// console.log(valores) // Imprimi o que tem as variaveis retiradas de calculo tranformando em objetos de javaScript ou melhor JSON